import styled from 'styled-components';
import { StyledImageLazyProps } from './ImageLazy.types';

export const StyledImageLazy = styled.img<StyledImageLazyProps>`
  max-width: 100%;
  border: 1px solid #6e6e735e;
`;
